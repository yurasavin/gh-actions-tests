name: Set and read env
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on: [push]

jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Print date
        run: echo "The date is $(date -u +'%Y-%m-%d %H:%M:%S %Z')"
